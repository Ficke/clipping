node_modules/
dist/
.astro/
.DS_Store
*.log

# Photo originals live in S3 (adamficke-com-originals), not git.
# `bun run photos:pull` hydrates them; markdown stays in git.
content/albums/**/*.jpg
content/albums/**/*.jpeg
content/albums/**/*.png
content/albums/**/*.webp
content/albums/**/*.avif

# Terraform (the .terraform.lock.hcl provider lockfile IS committed)
infra/.terraform/
infra/*.tfstate*
infra/tfplan

# preview-check screenshots
.claude/skills/preview-check/shots/
